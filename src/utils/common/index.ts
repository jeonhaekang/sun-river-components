import {
  getObjectEntries,
  getObjectKeys,
  getObjectValues
} from "./getObjectProperties";
import { toKebabCase } from "./toKebabCase";

const common = {
  getObjectEntries,
  getObjectKeys,
  getObjectValues,
  toKebabCase
};

export { common };
