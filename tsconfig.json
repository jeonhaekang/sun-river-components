{
  "compilerOptions": {
    "target": "ES5", // 출력할 ECMAScript 버전
    "module": "ESNext", // 모듈 시스템
    "lib": ["DOM", "ES6", "DOM.Iterable"], // 포함할 기본 타입 정의 라이브러리
    "allowJs": true, // JavaScript 파일의 컴파일 허용
    "jsx": "react-jsx", // JSX 지원
    "sourceMap": true, // 소스 맵 생성
    "outDir": "./dist", // 출력 디렉토리
    "strict": true, // 엄격한 타입 검사 옵션 활성화
    "esModuleInterop": true, // ES 모듈과의 상호 운용성 활성화
    "skipLibCheck": true, // 선언 파일(.d.ts)의 타입 검사 건너뛰기
    "forceConsistentCasingInFileNames": true, // 대소문자 일관성 강제
    "moduleResolution": "node", // 모듈 해석 전략 (기본값은 "Classic"이지만 대부분의 경우 "Node"를 사용)
    "resolveJsonModule": true, // JSON 모듈 지원
    "isolatedModules": true, // 각 파일을 독립된 모듈로 취급
    "noEmit": true, // 출력 파일을 생성하지 않음 (Babel 또는 Rollup 등과 함께 사용하는 경우 유용)
    "declaration": true,
    "declarationDir": "./dist",
    "baseUrl": "src", // 비 상대적 모듈 이름 해석을 시작하는 기본 디렉토리
    "paths": {
      // 모듈 이름과 경로 매핑 (alias)
      "@components/*": ["components/*"],
      "~/*": ["./*"]
      // 원하는 alias 추가
    }
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
